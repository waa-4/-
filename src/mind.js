const clamp=v=>Math.max(0,Math.min(1,v));
export function newMind(){return{curiosity:Math.random(),bravery:.2+Math.random()*.75,social:Math.random(),aggression:Math.random()*.45,energyBias:.3+Math.random()*.5,needs:{energy:.9,hunger:.1,social:.5},emotion:{fear:.05,joy:.5,stress:.05},goal:'wander',target:null,memory:[],timer:0}}
export function remember(e,kind,p,note=''){e.mind.memory.unshift({kind,note,x:p.x.toFixed(1),z:p.z.toFixed(1),age:0});e.mind.memory=e.mind.memory.slice(0,18)}
function nearestObj(w,e,kind){let b=null,d=1e9;for(const o of w.objects){if(o.kind!==kind)continue;let q=e.position.distanceToSquared(o.position);if(q<d){d=q;b=o}}return b}
function nearestEnt(w,e,pred){let b=null,d=1e9;for(const x of w.entities){if(x===e||!pred(x))continue;let q=e.position.distanceToSquared(x.position);if(q<d){d=q;b=x}}return b}
export function think(e,w,dt){let m=e.mind;m.timer-=dt;m.needs.energy=clamp(m.needs.energy-dt*.003*(1+m.energyBias));m.needs.hunger=clamp(m.needs.hunger+dt*.004);m.needs.social=clamp(m.needs.social-dt*.002);m.memory.forEach(x=>x.age+=dt);let danger=w.disaster!=='calm';m.emotion.fear=clamp(m.emotion.fear+(danger?dt*.1:-dt*.04));m.emotion.stress=clamp(m.emotion.stress+(danger?dt*.08:-dt*.03));m.emotion.joy=clamp(.35+m.needs.energy*.35-m.emotion.stress*.4);if(m.timer>0)return;m.timer=.4+Math.random()*.6;let food=nearestObj(w,e,'food'),shelter=nearestObj(w,e,'shelter'),friend=nearestEnt(w,e,x=>x.type.id!=='predator'),prey=e.type.id==='predator'?nearestEnt(w,e,x=>x.type.id!=='predator'):null;if(danger&&shelter&&m.bravery<.7){m.goal='shelter';m.target=shelter.position.clone();remember(e,'danger',e.position,w.disaster)}else if(prey&&m.aggression>.25){m.goal='chase';m.target=prey.position.clone()}else if(food&&m.needs.hunger>.45){m.goal='food';m.target=food.position.clone()}else if(friend&&m.social>.55&&m.needs.social<.45){m.goal='social';m.target=friend.position.clone()}else if(m.needs.energy<.25){m.goal='rest';m.target=null}else{m.goal='wander';m.target=w.v3((Math.random()*2-1)*19,e.position.y,(Math.random()*2-1)*19)}}